name: Daily Data Population

on:
  schedule:
    # Run daily at 2 AM UTC (when rate limits reset)
    - cron: '0 2 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  populate-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Print debug info
        run: |
          echo "Current directory: $(pwd)"
          echo "Listing files:" && ls -al
          echo "Environment variables:" && printenv
      - name: Write Elasticsearch CA cert
        run: |
          mkdir -p elasticsearch-8.13.0/config/certs
          echo "${{ secrets.ELASTIC_CA_CRT }}" > elasticsearch-8.13.0/config/certs/http_ca.crt

      - name: Set CA cert path env var
        run: echo "ELASTIC_CA_CERT_PATH=elasticsearch-8.13.0/config/certs/http_ca.crt" >> $GITHUB_ENV
      - name: Download latest Land Registry CSV
        run: |
          YEAR=$(date +"%Y")
          MONTH=$(date +"%m")
          CSV_URL="https://landregistry.data.gov.uk/data/ppd/monthly/file-${YEAR}-${MONTH}.csv"
          CSV_DIR="data/land-registry/${YEAR}"
          CSV_PATH="$CSV_DIR/pp-${YEAR}-${MONTH}.csv"
          mkdir -p "$CSV_DIR"
          if [ ! -f "$CSV_PATH" ]; then
            echo "Downloading $CSV_URL ..."
            curl -fSL "$CSV_URL" -o "$CSV_PATH" || echo "Warning: Could not download $CSV_URL"
          else
            echo "$CSV_PATH already exists."
          fi
      - name: Update Elasticsearch data
        env:
          ES_CLOUD_ID: ${{ secrets.ES_CLOUD_ID }}
          ES_NODE: ${{ secrets.ES_NODE }}
          ES_USERNAME: ${{ secrets.ES_USERNAME }}
          ES_PASSWORD: ${{ secrets.ES_PASSWORD }}
          ES_API_KEY: ${{ secrets.ES_API_KEY }}
          ES_INDEX: ${{ secrets.ES_INDEX }}
        run: npm run update-es-incremental 